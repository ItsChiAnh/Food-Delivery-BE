import orderModel from "../models/order.models.js";

const addOrder = async (req, res) => {
  res.send("add food function");
};

const removeOrder = async (req, res) => {
  res.send("add food function");
};

export { addOrder, removeOrder };
